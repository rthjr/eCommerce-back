# NOTE: keep this file aligned with application.yml to avoid confusing overrides.
spring.application.name=PAYMENT-SERVICE
server.port=8089

# Payment gateway (Python service running on :8976)
payment.gateway.url=http://localhost:8976
